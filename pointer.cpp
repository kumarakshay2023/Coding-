#include<iostream>
using namespace std;
int main(){
    int num=5;
    int *p=&num;
    /*
    // int *q=0;
    // q=&num;  both *p and *q are same 
    // cout<<p<<endl;
    // cout<<&num; // same addrees will print for both 
    // cout<<num<<endl;
    // cout<<*p; // derefrence operator uss adrress pr jo value hai usko print kr do
    cout<<sizeof(p)<<endl;
    cout<<sizeof(num);
    int *ptr// pointer to int is created and pointed to some garbage address
    // never do this bad practice
    */
   /*
     coyping a pointer
    int *q=p;
    cout<<p<<" - "<<q<<endl;
    cout<<*p<<" - "<<*q;
    */
}